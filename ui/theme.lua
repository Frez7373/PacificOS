local C = colors

-- PacificOS Classic theme: inspired by Windows 98 / early Windows NT.
-- Keep blue for title bars and selected controls, grey for the workspace.
return {
  bg = C.lightGray,
  panel = C.gray,
  panel2 = C.white,
  card = C.lightGray,
  card2 = C.gray,
  text = C.black,
  textOnBlue = C.white,
  muted = C.gray,
  accent = C.blue,
  accent2 = C.lightGray,
  good = C.green,
  warn = C.yellow,
  bad = C.red,
  dark = C.blue,
  border = C.gray,

  white = C.white,
  black = C.black,
  shadow = C.gray,
  highlight = C.white,
  face = C.lightGray
}
